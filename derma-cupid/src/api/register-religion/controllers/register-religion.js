'use strict';

/**
 *  register-religion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::register-religion.register-religion');
